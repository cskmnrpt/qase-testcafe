import { qase } from "testcafe-reporter-qase/qase";

// Please, change the Id from `1` to any case Id present in your project before uncommenting the test.

/* // Delete line, to uncomment

fixture`Example: idTest.js`.page`http://devexpress.github.io/testcafe/example/`;

const q = qase.id(1).create();
test.meta(q)('Test with Qase Id', async (t) => {
  await t.expect(true).ok();
});

*/ // Delete line, to uncomment
